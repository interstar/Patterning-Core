# Patterning x Clay (experiment)

This is a small experiment that renders a Patterning SVG inside a Clay document.

## Run

From this directory:

```bash
clj -M:clay notebooks/patterning_demo.clj
```

Clay will build an HTML document from the namespace and open it in your browser.
